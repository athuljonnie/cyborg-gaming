<html>
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DotGothic16&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css?family=Nunito+Sans:400,400i,700,900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  </head>
    <style>
      body {
        background-image: url('/shop/img/8481.jpg');         
            color: rgb(220, 154, 0);
        font-family: 'DotGothic16', sans-serif;
        text-align: center;
        margin-top: 40px ;
      
      }
        h1 {
          color: #88B04B;
          font-weight: 900;
          font-size: 40px;
          margin-bottom: 10px;
        }
        p {
          color: #ffffff;
          
          font-size:20px;
          margin: 0;
        }
      i {
        color: #9ABC66;
        font-size: 100px;
        line-height: 200px;
        margin-left:-15px;
      }
      .card {
        background: rgba(0, 0, 0, 0.314);
        padding: 60px;
        border-radius: 16px;
        display: inline-block;
        margin: 0 auto;
      }
    </style>
    <body>
      <div id="countdown-timer">
        Redirecting in <span id="countdown">6</span> seconds...
      </div>
      <div class="card">
      <div style="border-radius:200px; height:200px; width:200px; background: #F8FAF5; margin:0 auto;">
        <i class="checkmark">✓</i>
      </div>
        <h1>Order Placed!</h1> 
        <p>We received your order;<br/> happy shopping</p>
        <a href="/">
        <button class="btn btn-outline-success" action ='/'>Go to Home</button>
      </a>
      </div>
      <script>
        setTimeout(function() {
    window.location.href = '/orders'; // Replace '/orders' with the URL of the orders page
    }, 5000);
        // Function to update the countdown timer
        function updateTimer() {
          const countdown = document.getElementById("countdown");
          let seconds = parseInt(countdown.innerText);
        
          if (seconds > 0) {
            seconds--;
            countdown.innerText = seconds;
            setTimeout(updateTimer, 1000); // Update the timer every second (1000 milliseconds)
          } else {
            window.location.href = "/orders"; // Redirect when the timer reaches 0
          }
        }
        
        // Start the timer when the page loads
        window.onload = function() {
          updateTimer();
        };
        </script>


    </body>
</html>

